# SSD Studio — CereFlow agent + self-healing monitor dependencies
# Python 3.11+

mcp[cli]>=1.2.0
asyncpg>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Optional: real-time agent voice/transport
livekit>=0.11.0
